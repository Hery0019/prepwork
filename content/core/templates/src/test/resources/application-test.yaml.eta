<% /* Profil Spring `test`, activé par surefire/failsafe (spring.profiles.active=test). */ -%>
<%
const base = { logging: { level: { root: 'WARN', [it.project.basePackage]: 'INFO' } } };
-%>
# <%= it.d('Généré par prepwork. Activé par Maven pour tous les tests (spring.profiles.active=test).', 'Generated by prepwork. Activated by Maven for every test (spring.profiles.active=test).') %>
<%~ it.yaml(it.merge(base, it.properties.test)) %>
