# <%= it.d("Généré par prepwork.", "Generated by prepwork.") %>

# <%= it.d("Environnement et secrets : `.env.example` est commité, `.env` jamais (CORE-030).", "Environment and secrets: `.env.example` is committed, `.env` never (CORE-030).") %>
.env
.venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# <%= it.d("Caches d'outillage.", "Tooling caches.") %>
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
