<%= it.t("// La racine renvoie vers la première feature ; aucune logique ne vit ici (NEXT-002).", "// The root redirects to the first feature; no logic lives here (NEXT-002).") %>
import { redirect } from 'next/navigation';

export default function HomePage() {
  redirect('/notes');
}
