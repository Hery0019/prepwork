<%= it.t("// SPA-001 et SPA-012 : la surface publique de la feature. `app/` n'importe que ça.", "// SPA-001 and SPA-012: the public surface of the feature. `app/` imports only this.") %>
export { NoteDetail } from './ui/NoteDetail';
export { NoteForm } from './ui/NoteForm';
export { NoteList } from './ui/NoteList';
