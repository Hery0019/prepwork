# <%= it.d('Glossaire', 'Glossary') %>

<%= it.d("Vocabulaire métier du projet (CORE-042). Un concept, un terme, une forme anglaise utilisée telle quelle dans le code. Ce fichier appartient à l'équipe : prepwork ne le modifie jamais.", 'Business vocabulary of the project (CORE-042). One concept, one term, one English form used verbatim in the code. This file belongs to the team: prepwork never touches it.') %>

| <%= it.d('Terme', 'Term') %> | <%= it.d('Identifiant (code)', 'Identifier (code)') %> | <%= it.d('Définition', 'Definition') %> |
|---|---|---|
| | | |
