# ADR 0003 — Database

Date : 2026-09-09 · Status : accepted

## Context

Choice made in the prepwork questionnaire, before the first line of business code.

## Decision

**none**

## Consequences

With no database, the reference example's repository is an in-memory list; moving to a database is a change in `scaffold.yaml`, followed by `prepwork sync`.
