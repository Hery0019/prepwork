---
name: "testing"
description: "The three test levels, Testcontainers, naming. Read before writing or changing a test."
---

<!-- Generated by prepwork 0.1.0 from `scaffold.yaml`. Do not edit: change the source, then run `prepwork sync`. -->

# Testing

Three levels, not one more. The right level is the cheapest one that genuinely exercises the behaviour.

Marker after the identifier: a tool name = tooled constraint (the build or the commit fails); `guidance` = rule of conduct checked in review.

## Base rules

### Rules

- **CORE-020** · guidance — There are exactly three test levels, unit (no Spring context), slice (`@WebMvcTest`, `@DataJpaTest`) and integration (`@SpringBootTest` with Testcontainers).
  Why: Each level has a cost and a purpose; a fourth category is where confusion starts.
- **CORE-021** · `archunit` — Slice and integration tests run against the real database engine through Testcontainers; H2 is never used.
  Why: SQL dialects differ; a test that passes on H2 proves nothing about production.
- **CORE-022** · guidance — Test methods are named `method_condition_expectedResult`.
  Why: A failing test name must say what broke without opening the file.
- **CORE-023** · guidance — No numeric coverage threshold is imposed; a test is judged on what it verifies.
  Why: A percentage target produces assertion-free tests written to satisfy the metric.
- **CORE-024** · guidance — A unit test never starts a Spring context; dependencies are handed in as plain objects or mocks.
  Why: Unit tests must run in milliseconds to be run constantly.
- **CORE-025** · guidance — Every feature ships with at least one unit test, plus the slice test of each controller or repository it adds.
  Why: The reference example shows one test per level; new code follows the same shape.

### Anti-patterns

- **CORE-AP-020** · guidance — Using `@SpringBootTest` for every test because it "always works".
  Why: The suite becomes minutes long and nobody runs it before pushing.
  Instead: Pick the cheapest level that exercises the behaviour under test.
- **CORE-AP-021** · guidance — Replacing the database with H2 or an in-memory fake in a persistence test.
  Why: The test validates the fake, not the SQL that will run in production.
  Instead: A `@DataJpaTest` on the Testcontainers database.
- **CORE-AP-022** · guidance — Test names such as `test1`, `shouldWork` or `testCreate`.
  Why: The name carries no information when it appears in a failure report.
  Instead: `create_titleBlank_returns400`: method, condition, expected result.
