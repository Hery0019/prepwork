# Environment variables of the application (CORE-046). Copy to `.env`, never commit `.env`.
# Anything prefixed with VITE_ ships in the bundle and is therefore public (CORE-070).

VITE_API_BASE_URL=http://localhost:8080/api/v1
